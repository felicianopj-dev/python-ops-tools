# Development dependencies (testing + linting).
# Runtime deps live in requirements.txt; install both for local development:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=7.0
ruff>=0.4
